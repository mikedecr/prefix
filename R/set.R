#' index(a, b) = a[b]
#' @export
index = `[`

#' item(a, b) = a[[b]]
#' @export
item = `[[`

#' isin(a, b) = a %in% b
#' @export
isin = `%in%`

# other set operators like union and intersect already defined =)

