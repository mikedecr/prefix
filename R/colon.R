#' Prefix function binding for `:`
#'
#' `iota(a, b)` is equivalent to `a : b` for creating regular sequences of numbers.
#' `iota` does not support the use of `:` for interaction terms in regression formulae.
#' @usage NULL
#' @export
iota = `:`

