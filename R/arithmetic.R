

#' Prefix function binding for `+`
#'
#' `add(a, b)` is equivalent to `a + b`.
#' @usage NULL
#' @export
add = `+`

#' Prefix function binding for `-`
#'
#' `minus(a, b)` is equivalent to `a - b`.
#' @usage NULL
#' @export
minus = `-`

#' Prefix function binding for `*`
#'
#' `mult(a, b)` is equivalent to `a * b`.
#' @usage NULL
#' @export
mult = `*`

#' Prefix function binding for `/`
#'
#' `div(a, b)` is equivalent to `a / b`.
#' @usage NULL
#' @export
div = `/`

#' Prefix function binding for `^`
#'
#' `pow(a, b)` is equivalent to `a ^ b`.
#' @usage NULL
#' @export
pow = `^`

#' Prefix function binding for `%%`
#'
#' `mod(a, b)` is equivalent to `a %% b`.
#' @usage NULL
#' @export
mod = `%%`

#' Prefix function binding for `%/%`
#'
#' `floordiv(a, b)` is equivalent to `a %/% b`.
#' @usage NULL
#' @export
floordiv = `%/%`

#' Prefix function binding for `%*%`
#'
#' `dot(a, b)` is equivalent to `a %*% b`.
#' @usage NULL
#' @export
dot = `%*%`

# kronecker and outer already defined =)

